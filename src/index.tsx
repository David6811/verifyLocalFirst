import React from 'react';
import ReactDOM from 'react-dom/client';
import LocalfirstTest from './components/LocalfirstTest';
const root = ReactDOM.createRoot(
  document.getElementById('root') as HTMLElement
);
root.render(
  <React.StrictMode>
    <LocalfirstTest />
  </React.StrictMode>
);
